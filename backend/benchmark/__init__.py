"""
AMPS Benchmark Comparison Layer.

Modules:
  comparison  — scoring engine: scores both outputs, builds BenchmarkComparison
  runner      — orchestrates generalist execution alongside specialist execution

Entry point: runner.run_generalist_comparison(task, store, registry) -> BenchmarkComparison
"""
